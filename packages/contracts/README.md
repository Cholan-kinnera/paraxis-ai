# @paraxis/contracts

Shared Pydantic models, JSON schema contracts, and event definitions shared across Django Core, FastAPI Intelligence, and the Next.js frontend.

## Purpose
- Eliminates schema drift between services.
- Defines standard request and response envelopes.
- Houses shared event payloads (`campus:events`, `incident:stream`).
