# Infrastructure Configurations — Paraxis AI

This directory houses infrastructure configuration templates:
- `docker/`: Base container build recipes.
- `postgres/`: Database initialization scripts (`init-pgvector.sql`).
- `redis/`: Redis persistence and caching configurations.
- `deployment/`: Cloud-agnostic manifests (Helm, ECS, Cloud Run templates).
